{
  "$schema": "https://railway.com/railway.schema.json",
  "build": {
    "builder": "RAILPACK",
    "command": "pip install -r requirements.txt && cd backend && python manage.py collectstatic --noinput"
  },
  "deploy": {
    "startCommand": "cd backend && python manage.py migrate --noinput && gunicorn -w 2 -b 0.0.0.0:$PORT config.wsgi:application"
  }
}
